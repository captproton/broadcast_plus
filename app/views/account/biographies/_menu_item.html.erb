<% if can? :read, Biography.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :biographies]),
    label: t('biographies.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('biographies.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
