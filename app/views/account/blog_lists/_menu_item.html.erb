<% if can? :read, BlogList.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :blog_lists]),
    label: t('blog_lists.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('blog_lists.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
