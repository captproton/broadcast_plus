<% if can? :read, Book.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :books]),
    label: t('books.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('books.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
