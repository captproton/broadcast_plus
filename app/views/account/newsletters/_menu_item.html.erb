<% if can? :read, Newsletter.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :newsletters]),
    label: t('newsletters.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('newsletters.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
