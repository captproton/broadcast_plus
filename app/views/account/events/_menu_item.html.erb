<% if can? :read, Event.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :events]),
    label: t('events.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('events.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
