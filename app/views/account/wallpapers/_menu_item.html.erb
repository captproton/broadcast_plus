<% if can? :read, Wallpaper.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :wallpapers]),
    label: t('wallpapers.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('wallpapers.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
