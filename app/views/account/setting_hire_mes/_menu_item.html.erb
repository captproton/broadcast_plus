<% if can? :read, SettingHireMe.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :setting_hire_mes]),
    label: t('setting_hire_mes.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('setting_hire_mes.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
