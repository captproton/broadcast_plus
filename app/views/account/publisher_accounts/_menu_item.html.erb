<% if can? :read, PublisherAccount.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :publisher_accounts]),
    label: t('publisher_accounts.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('publisher_accounts.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
