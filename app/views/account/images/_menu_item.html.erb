<% if can? :read, Image.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :images]),
    label: t('images.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('images.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
