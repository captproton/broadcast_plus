<% if can? :read, SettingPodcastPage.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :setting_podcast_pages]),
    label: t('setting_podcast_pages.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('setting_podcast_pages.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
