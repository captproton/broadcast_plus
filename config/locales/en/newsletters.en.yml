en:
  newsletters: &newsletters
    label: &label Newsletters
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Newsletter
      create: Create Newsletter
      edit: Edit Newsletter
      update: Update Newsletter
      destroy: Remove Newsletter
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{newsletter_name}? This will also remove it's associated data. This can't be undone.
    fields: &fields
      id:
        heading: Newsletter ID
      site_id:
        heading: Site ID
      title:
        _: &title Title
        label: *title
        heading: *title
      mailing_group:
        _: &mailing_group Mailing Group
        label: *mailing_group
        heading: *mailing_group
      sender:
        _: &sender Sender
        label: *sender
        heading: *sender
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Newsletters"
      index: "List Newsletters"
      create: "Add a New Newsletter"
      member_actions: "Actions for an Individual Newsletter"
      show: "Retrieve a Newsletter"
      update: "Update a Newsletter"
      destroy: "Delete a Newsletter"
      fields: *fields
    index:
      section: "%{sites_possessive} Newsletters"
      contexts:
        site:
          header: Newsletters
          description: Below is a list of Newsletters that have been added for %{site_name}.
          description_empty: No Newsletters have been added for %{site_name}.
      fields: *fields
      buttons: *buttons
    show:
      section: "%{newsletter_name}"
      header: Newsletter Details
      description: Below are the details we have for %{newsletter_name}.
      manage_description: You'll also find options for updating these details or removing %{newsletter_name} from %{site_name} entirely.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "New Newsletter for %{site_name}"
      header: New Newsletter Details
      description: Please provide the details of the new Newsletter you'd like to add to %{site_name}.
      form: *form
    edit:
      section: "%{newsletter_name}"
      header: Edit Newsletter Details
      description: You can update the details or settings for %{newsletter_name} below.
      form: *form
    notifications:
      created: Newsletter was successfully created.
      updated: Newsletter was successfully updated.
      destroyed: Newsletter was successfully destroyed.
  account:
    newsletters: *newsletters
  activerecord:
    attributes:
      newsletter:
        title: *title
        mailing_group: *mailing_group
        sender: *sender
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
